# Dockerを使わずに `npm run dev` で起動する場合の設定です。
# Docker Compose起動時は API_BASE_URL=http://backend:8080 をcompose側で設定するため、このファイルは不要です。
API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
